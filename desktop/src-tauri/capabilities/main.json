{
  "identifier": "main",
  "description": "Capabilities for the main window (created at runtime). The renderer is loaded from the sidecar origin (http://127.0.0.1:*), so remote URLs must be allowed for plugin commands such as the dialog folder picker.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:*"]
  },
  "permissions": [
    "core:default",
    "core:event:default",
    "dialog:default",
    "notification:default",
    "allow-app-version",
    "allow-check-for-updates",
    "allow-download-and-install-update"
  ]
}
